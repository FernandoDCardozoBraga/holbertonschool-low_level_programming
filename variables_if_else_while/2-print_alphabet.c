#include <stdio.h>
/**
 * main - Entry point
 * Description: Assign a random number to the variable n.
 * Retour: Always 0 (Success)
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	putchar(i);
	putchar(\n)
	return (0);
}
